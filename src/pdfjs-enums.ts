export enum SidebarView {
    UNKNOWN = -1,
    NONE,
    THUMBS,
    OUTLINE,
    ATTACHMENTS,
    LAYERS,
}

export enum ScrollMode {
    UNKNOWN = -1,
    VERTICAL,
    HORIZONTAL,
    WRAPPED,
    PAGE
}

export enum SpreadMode {
    UNKNOWN = -1,
    NONE,
    ODD,
    EVEN
}
